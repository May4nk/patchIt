export interface userdatatypes {
  email?: string;
  password?: string;
  username?: string;
}

export interface loginproptypes {
  showLogin: boolean;
  setShowLogin: any; 
}

export interface usertype {
  id: number;
  email: string;
  username: string;
}
